baseURL: https://transtrav.ch/
defaultContentLanguage: fr
title: transtrav.ch

theme: ["PaperMod"]

params:
  homeInfoParams:
    Title: Bienvenue sur transtrav.ch!
    Content: Un blog de cinéma super slay, sans ligne directrice précise, sans restrictions, juste un max de kiff — ou de rage — à parler de cinéma.

caches:
  images:
    dir: :cacheDir/images

menu:
      main:
      - identifier: "contact"
        name: "contact"
        url: "mailto:coucou@transtrav.ch"
        weight: 10